# 版本更新紀錄

這裡記錄**會影響你怎麼操作、怎麼讀結果**的更新。純文字修正與不影響分析結果的內部整理不逐項列出。

---

## v1.1.0 — 2026-09-10 ｜ 影響結果

這一版修正了幾處「說明是對的，但程式沒有照著做」的地方，以及一個計算式的錯誤。

### 分析流程

- **PCA 變異解釋比例**（`02_cluster.R`）：改用特徵值（標準差的平方）計算。原本用標準差本身相除，
  算出來的是「標準差佔比」，不是變異解釋比例。`npc <- 25` 沒有改，所以分群結果不受影響，
  但腳本印出的量化 cutoff 數字會不一樣。
- **Doublet 判定**（`03_annotate.R`）：拆成「候選」與「確認」兩層。原本只要 doublet 比例 > 0.5
  就整群移除；現在候選群要經過三項診斷確認後才會被刪，未確認的會留下並跳出提醒。
- **增殖細胞的分類**（`03_annotate.R`）：修正 `Cycling (undetermined)` 被歸進 `Glial (undetermined)`
  的問題。譜系（`celltype_l1`）與狀態（`celltype_l3`）現在分成兩欄——增殖是狀態，不是細胞型別。
- **軌跡分析**（`08_trajectory.R`）：Slingshot 若找出一條以上的 lineage 會停下來並說明原因。
  原本會只取第一條、且把所有細胞的權重設為 1，那和 Slingshot 的結果並不一致。
- **活性分數的存放方式**（`09_activity.R`）：PROGENy 與 SCENIC 的分數改存進 `data` 而不是 `counts`。
  它們是推算出來的連續分數（可能為負），不是計數。

### 統計解讀

- **GSEA 熱圖**（`06a_pseudobulk_gsea.R`）：NES 照樣上色，但不顯著的格子會調淡並標示星號。
  原本不顯著的格子跟顯著的長得一樣，容易被讀成「也有這條路徑，只是弱一點」。
- **配對樣本太少時**（`06a`）：少於 4 對病人的結果會在摘要表標成 exploratory-only。
  程式仍會跑完，因為「軟體能產生 p 值 ≠ 實驗設計足夠」本身就是要教的事。
- **細胞數不足時不做檢定**（`09_activity.R`）：任一格惡性細胞少於 20 顆時，
  不再輸出配對 t 檢定，改為只畫描述性的配對圖。原本只印警告、p 值照樣輸出。
- **反卷積比例的命名**（`10_deconv_survival.R`）：`mac_hi` 改名為 `imm_hi`，
  輸出檔改為 `10_km_immune.pdf`。那個欄位是免疫細胞的**總**比例，不是巨噬細胞比例。
  同時新增以連續比例為主的 Cox 模型（中位數切兩半只用於畫 KM 圖）。
- **LISI 的解釋**（投影片 Q3、題庫）：改為「支持混合程度較好／支持病人特異結構被保留」。
  LISI 量的是鄰域裡病人有多混，不能單獨當成整合正確或 CNV 被保留的證據。

### 課程內容

- **`02_cluster.R` 新增 §4c「什麼時候刪、刪到什麼程度」**：doublet 的處理沒有單一答案，
  這一節把三個決定因素攤開來——平台（液滴式 vs 孔盤式）、有沒有先判定惡性、
  以及各個下游分析的敏感度（通訊與軌跡很敏感、pseudobulk DE 相對耐受）。
  `07_cellchat.R` 與 `08_trajectory.R` 開頭也加了提醒。
- 題庫新增 2 題（Q2 章），共 **90 題**：doublet 什麼時候刪、哪些下游分析最敏感。
- 練習題新增〈**關於「跟原論文對答案」**〉：重新分析不必得到跟原論文一樣的數字。
  結果一致、部分一致、有證據支持的不一致，三種都算完成——但不可以為了貼近原文而回頭調參數。
- 題庫調整三題的說法：QC 指標改成「通常右偏」而非「對數常態分布」、
  apeglm 的收縮強度改成「在這份資料上」而非通則、LISI 如上。

### 我需要重跑嗎？

用舊版跑過以下章節的話，建議重跑：

- `02_cluster.R`（只有印出的 cutoff 數字會變，分群結果不變）
- `03_annotate.R`（細胞型別欄位的內容會變）
- `06a_pseudobulk_gsea.R`（熱圖與摘要表）
- `08_trajectory.R`、`09_activity.R`、`10_deconv_survival.R`

`00`、`01`、`04`、`05`、`06b`、`07` 沒有改動，不需要重跑。

---

## v1.0.0 — 2026-09-07

Q 系列首次公開：三集投影片（中英各一套，共 228 頁）、12 支 R 腳本（完整版與練習版）、
23 題實作練習、88 題自測題庫、4 份參考庫。
