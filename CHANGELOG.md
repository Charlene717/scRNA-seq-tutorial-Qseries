# 版本更新紀錄

這裡記錄**會影響你怎麼操作、怎麼讀結果**的更新。純文字修正與不影響分析結果的內部整理不逐項列出。

---

## v1.2.0 — 2026-09-12 ｜ 影響結果

這一版是把 00–10 整條流程實跑一遍之後的修正。大部分是「腳本對、但參考數字或教材說明沒跟上」，
另外撤回了 v1.1.0 的一個決定。

### 撤回 v1.1.0 的細胞標籤改名

- **`07_cellchat.R` 不再把 `Immune cell` 改名成 `Macro/MG`**，改為直接沿用 `06a` 建立的 `type` 欄位。
  v1.1.0 加的那個改名（以及它附帶的 marker 自我檢查）在這一版整段移除，理由有兩個：
  ① 這份資料的免疫細胞從未做過亞群分群，沒有任何一步產生過「這些是髓系」的證據；
  ② 那個檢查的位置不對——它若判定該換標籤，`06a` 的 DE 與富集早已用舊標籤跑完並寫進 `output/`，
  改了就會讓兩節的結果對不起來。一個「失敗了也沒辦法照做」的檢查不是檢查。
  解析度的說明改放在標籤進來的地方（`04_multipatient.R` §2），那裡改還來得及。
  **通訊分析的圖上現在寫 `Immune cell`，不是 `Macro/MG`**；網路數值不變，只有群名變了。
  重跑 `07` 之前請刪掉 `output/rds/07_cellchat/`（腳本也會自行偵測群名不符並重算）。

### 分析流程

- **`08_trajectory.R` 的參考數字已用分支版重跑補齊**，v1.1.0 標的「待重跑更新」解除。
  排序仍用 `waldStat`（本例前 11 個基因的 p 值都下溢成 0，waldStat 卻從 1,129 到 106）。
  重要結果：Slingshot 找出**兩條 lineage 且走向相反**——lineage 1 的 Neftel OPC 0.437 → −0.383、
  MES 0.764 → 1.554；lineage 2 則是 OPC 0.389 → 0.604、MES 0.858 → 0.436。
  `diffEndTest` 有 323／2,000 個基因終點差異達 FDR < 0.05。
  所以「OPC 樣 → MES 樣」只對 lineage 1 成立，教材已全面收斂到這個範圍。
- **`09_activity.R`**：條件比較不足時只畫描述圖、不做檢定的那一段，現在會把圖存成
  `09_progeny_hypoxia_paired.pdf`（先前只畫在螢幕上）。`vital_status` 的交叉表加 `useNA = "ifany"`
  （`10`），原本會把 `NA` 那一位藏起來、表格加總對不上病人數。
- **`10_deconv_survival.R`**：`GDCprepare` 會自動重試三次。它和 `GDCdownload` 是兩件事——
  檔案下載完之後 prepare 還要再連一次 GDC API 補臨床欄位，那支 API 不穩，失敗時的訊息
  看起來像腳本壞了。現在失敗會明講「檔案沒問題、過幾分鐘從這一行重跑即可」。
- **`06b_cell_level_de.R`**：一致性防線的輸出分母改成「實際檢查過的基因數」（30），
  原本拿整張 DE 表的列數（7,648）當分母，會讀成「檢查了七千多個全部沒過」。

### 練習版與練習題

- **`tools/make_exercises.py` 修好五條失效的規則。** 完整版改寫之後這幾條再也比對不到任何一行，
  等於有五道練習題在無人察覺的情況下消失了（取幾個 PC、inferCNV 的 cutoff、CNV 的兩條閾值，
  以及一條重複的規則）。挖空數 44 → 49。其中 `cutoff` 那條若不修，下一次重生練習版時
  答案會連同註解一起印在題目上。
- 新增 `05-5`（CNV 閾值的分位數是判斷不是定律，換一組重跑比較三類細胞的差異）。
- `07_cellchat.R` 的練習 `7-7`／`7-8` 原本在練習版裡重複出現兩次，已修正。
- 修正多處練習題與註解的投影片頁碼。

### 文字與註記

- **`03_annotate.R`**：群 0 的 marker 註解 `C1QL4` 改回 `C1QL1`。C1QL4 依 Human Protein Atlas
  是睪丸特異（腦部蛋白質層級未偵測到），不可能是 GBM 的膠質群 marker；C1QL1 則是腦部增強、
  歸在寡樹突髓鞘化表現群。
- 多處實跑數字補正：`05` 的細胞數 3,589 → 3,539（3,589 是下載時的原始數，`04` 濾掉 50 顆）、
  `06a` 的 cell-level 顯著基因數以實跑 3,486 為準。

### 投影片與講稿

投影片與講稿有多處同步修正（Q2 四處、Q3 十七處），其中部分與已上線影片的旁白不一致。
勘誤清單見專案內的待補錄檔案；投影片 PDF 皆已重新輸出。

---

## v1.1.0 — 2026-09-10 ｜ 影響結果

這一版修正了幾處「說明是對的，但程式沒有照著做」的地方，以及一個計算式的錯誤。

### 分析流程

- **PCA 變異解釋比例**（`02_cluster.R`）：改用特徵值（標準差的平方）計算。原本用標準差本身相除，
  算出來的是「標準差佔比」，不是變異解釋比例。`npc <- 25` 沒有改，所以分群結果不受影響，
  但腳本印出的量化 cutoff 數字會不一樣。
- **Doublet 判定**（`03_annotate.R`）：拆成「候選」與「確認」兩層。原本只要 doublet 比例 > 0.5
  就整群移除；現在候選群要經過三項診斷確認後才會被刪，未確認的會留下並跳出提醒。
- **增殖細胞的分類**（`03_annotate.R`）：修正 `Cycling (undetermined)` 被歸進 `Glial (undetermined)`
  的問題。譜系（`celltype_l1`）與狀態（`celltype_l3`）現在分成兩欄——增殖是狀態，不是細胞型別。
- **軌跡分析**（`08_trajectory.R`）：Slingshot 在示範資料上會找出**不只一條 lineage**。
  舊版只取第一條、把所有細胞的權重設為 1，等於默默丟掉其他分支上的細胞卻不會告訴你。
  現在改成：先印出 lineage 的條數與組成，`fitGAM` 直接吃整個 pseudotime 矩陣與 curve weights，
  多一節 §1c 用 `diffEndTest()` 比較分支終點差不差得開。需要「單一條」的圖與早／晚比較
  仍固定用 lineage 1，圖名與輸出都標明是哪一條。
  **這會改變 `08_traj_association.csv` 的 waldStat 與名次**，腳本註解裡的幾個參考數字
  （TAGLN 91、GFAP 849、GPR37L1 4.94…）是舊版跑出來的，已標「待重跑更新」。
  不受影響的是只跟 pseudotime 有關的數字：Neftel 兩端分數、跨工具的 Spearman 相關。
  新增輸出：`08_pseudotime_lineages.pdf`、`08_traj_association_bylineage.csv`、`08_traj_diffend.csv`。
- **活性分數的存放方式**（`09_activity.R`）：PROGENy 與 SCENIC 的分數改存進 `data` 而不是 `counts`。
  它們是推算出來的連續分數（可能為負），不是計數。

### 統計解讀

- **GSEA 熱圖**（`06a_pseudobulk_gsea.R`）：NES 照樣上色，但不顯著的格子會調淡並標示星號。
  原本不顯著的格子跟顯著的長得一樣，容易被讀成「也有這條路徑，只是弱一點」。
- **配對樣本太少時**（`06a`）：少於 4 對病人的結果會在摘要表標成 exploratory-only。
  程式仍會跑完，因為「軟體能產生 p 值 ≠ 實驗設計足夠」本身就是要教的事。
- **細胞數不足時不做檢定**（`09_activity.R`）：任一格惡性細胞少於 20 顆時，
  不再輸出配對 t 檢定，改為只畫描述性的配對圖。原本只印警告、p 值照樣輸出。
- **反卷積比例的命名**（`10_deconv_survival.R`、Q3 頁 81–82）：`mac_hi` 改名為 `imm_hi`，
  輸出檔改為 `10_km_immune.pdf`。那個欄位是免疫細胞的**總**比例，不是巨噬細胞比例——
  參考組只分到 `Immune` 這一層，拆不出巨噬細胞。同時新增以連續比例為主的 Cox 模型
  （中位數切兩半只用於畫 KM 圖）。**投影片圖說、示意圖圖例與旁白也一起改了**：
  示意圖的分格改成與腳本一致的五格（惡性／免疫／寡樹突／血管／其他）。
  數字本身沒有變（HR 1.10、log-rank p = 0.5、年齡 HR 1.03），只有名稱。
  ⚠ 已上線影片的頁 81–82 音軌還是舊說法，勘誤同下。
- **重複基因符號的三步折疊**（`10_deconv_survival.R`）：新增一項檢查，印出「被折疊過的符號有幾個
  進到共同基因裡」，讓你能判斷折疊規則對結論有沒有影響，而不是假設它沒有。
  本例實測：1,233 個撞名列裡，用註解（`gene_type`）只拆得掉 7 列，其餘 1,226 列靠表現量折疊——
  原本註解寫「撞名多半在第②步就消失」，與實測不符，已更正。
- **LISI 的解釋**（投影片 Q3、題庫）：改為「支持混合程度較好／支持病人特異結構被保留」。
  LISI 量的是鄰域裡病人有多混，不能單獨當成整合正確或 CNV 被保留的證據。

### 課程內容

- **`02_cluster.R` 新增 §4c「什麼時候刪、刪到什麼程度」**：doublet 的處理沒有單一答案，
  這一節把三個決定因素攤開來——平台（液滴式 vs 孔盤式）、有沒有先判定惡性、
  以及各個下游分析的敏感度（通訊與軌跡很敏感、pseudobulk DE 相對耐受）。
  `07_cellchat.R` 與 `08_trajectory.R` 開頭也加了提醒。
- 題庫新增 2 題（Q2 章），共 **90 題**：doublet 什麼時候刪、哪些下游分析最敏感。
- 練習題新增〈**關於「跟原論文對答案」**〉：重新分析不必得到跟原論文一樣的數字。
  結果一致、部分一致、有證據支持的不一致，三種都算完成——但不可以為了貼近原文而回頭調參數。
- 題庫調整三題的說法：QC 指標改成「通常右偏」而非「對數常態分布」、
  apeglm 的收縮強度改成「在這份資料上」而非通則、LISI 如上。

### 措辭與方法定位

這些不改結果，但會影響你怎麼讀自己的輸出：

- **LISI**（`04_multipatient.R`）：改成「支持正常細胞混得比較開／支持病人特異結構被保留」。
  LISI 只量鄰域裡病人有多混，不能單獨證明整合正確，也不等於 CNV 被保住——那要看 inferCNV。
- **CellChat 的 `population.size = TRUE`**（`07_cellchat.R`、投影片、講稿）：原本寫「群大小校正」是反的。
  它是把族群豐度**納入**通訊機率，細胞多的群本來就會拿到較強的整體訊號。
  新增練習 7-7：TRUE / FALSE 各跑一次比較。
  ※ Q3 影片中這一句旁白仍是舊說法，之後會補正；以文字教材與本頁為準。
- **把 `Immune cell` 改名成 `Macro/MG`**（`07_cellchat.R`）：標明這是假設而非證據，
  並加上 PTPRC / CD68 / CSF1R / CD3E 的檢查與練習 7-8。
- **inferCNV 的「CNV 分數 + 相關」**（`05_infercnv.R`、投影片頁 25、講稿）：不再說成「Tirosh 2016 的做法」。
  兩個維度的想法確實來自 Tirosh 2016 / Neftel 2019，但本課的參考 profile 是先用 cnv.score 排序
  取前 200 顆自建的簡化版，不是原文分類器的忠實實作——原文比對的是該腫瘤已知的惡性 CNA profile。
  練習 5-2（200 改成 50 / 500）就是這個簡化的敏感度測試。
  ※ Q3 影片中這一句旁白仍是舊說法，之後會補正。
- **軌跡起點用的 Neftel 分數**（`08_trajectory.R`）：標明每組只有 6 個基因，是挑起點用的簡表，
  不是完整 signature（原文每組約 50 個），不要拿它報狀態比例。
- **SoupX 的 0.05 / 0.10**（`01_qc.R`）：標明是教學用的經驗門檻，會隨組織與解離品質變動。
- **cell-level DE 的「三道防線」**（`06b_cell_level_de.R`）：補一句說明「防線」是比喻——
  三項是診斷與壓力測試，修不好假重複，該報的還是 pseudobulk 的結果。
- **重複的基因 symbol**（`10_deconv_survival.R`）：說明直接去重是最粗糙的做法（留哪一個取決於列順序），
  並特別提醒**不要反射性地用 `rowsum()` 加總**——加總適用於「轉錄本 → 基因」，
  但這裡的重複是不同基因座撞名（蛋白編碼撞上假基因／lncRNA），加起來反而把不同的東西混成一個。
  程式改成三步（`§1b`）：① 先印出重複的組成（有幾個 `_PAR_Y`、其餘是什麼 `gene_type`）
  ② 用註解拆撞名——同一個符號底下若同時有蛋白編碼與非蛋白編碼，拿掉非蛋白編碼的那幾列
  （只動撞名的列，不會把沒撞名的 lncRNA 一起刪掉）③ 殘餘的留表現量最高的那一列。
  第③步的疑慮也寫在腳本裡：那是依賴資料的選擇，換一批病人可能留到不同基因座；
  若殘餘的是兩個真的不同的基因座，留一個等於默默刪掉另一個。
  **這一版改動會讓反卷積比例微幅改變，腳本裡的 HR 參考數字已標註待重跑更新。**
  同時說明「全程用 Ensembl ID 就沒事」並不成立——MuSiC 的參考組、msigdbr 的基因集、
  marker 清單都是用符號當索引，轉換只是被延後。練習 10-4 改成比較幾種做法。

### 驗證的獨立性與結果的定位

這一批修正的共同主題是：**別把「跟自己的假設一致」當成「被驗證了」。**

- **`05_infercnv.R` 的一致性數字不再叫 precision / recall。** 作者標註在這支腳本裡出現三次，
  都在做決定的那一側——挑 inferCNV 參考組、譜系否決、然後當標準答案。被拿來幫忙分類的資訊
  又被拿來評分，是循環的。現在改叫「一致率（concordance）」，並同時印**純 CNV 版**與
  **最終版**（含譜系否決）兩套數字，兩者之差就是作者標籤替分數加了多少。
  真正的獨立驗證要用沒參與分類的證據（配對基因體 CNV、突變、另一份獨立標註）。
- **`10_deconv_survival.R` 的存活結果全面降級為探索性。** 原本寫「兩組同樣被削，所以組間比較仍可報」
  ——這個推論不成立：刪除的機制取決於「有沒有死」，那 53 位存活者從沒進入他們本該參與的 risk set，
  KM/Cox 依賴的 non-informative censoring 假設站不住。年齡陽性對照仍然保留、仍然重要，
  但它只能支持「pipeline 大致合理」，**無法**排除選擇性缺失造成的偏誤。
  分析照跑（讓你看到「資料有嚴重問題，R 還是會吐 p 值給你」），但結論只能寫成
  「在 complete-case 子集裡沒有觀察到關聯」。
- **`08_trajectory.R` 的軌跡驗證拆成兩半讀。** 起點是用 OPC 分數挑的，所以「OPC 沿軌跡下降」
  有一部分是建構上就保證的，只能當內部一致性檢查；**MES 上升那一半才是獨立證據**，因為 MES
  沒有參與挑起點。下結論靠 MES 那一半。
- **`06a` 的探索性標記跟著結果走。** `06_de_*.csv` 與 `06_gsea_all_types.csv` 每一列都加上
  `n_pairs` 與 `analysis_status`。單獨打開檔案的人也看得到那份結果只有 2 對病人——
  限制不該留在另一個檔案裡等人自己去查。
- **`07_cellchat.R` 的 `Immune cell → Macro/MG` 現在會自我檢查。** 腳本會印 PTPRC / CD68 /
  CSF1R / CD3E 的陽性率，T 細胞標誌超過 10% 就跳警告要你改回 `Immune` 或先做亞群分群。
  假設寫在註解裡不算數，要跑得出來才算。

### 措辭：把經驗法則講成經驗法則

- `01_qc.R`：MAD 那行不再說「是對數常態」，改成「通常右偏，取 log10 後較接近對稱」。
- `01_qc.R`：DoubletFinder / scDblFinder 的 benchmark 引用修正——Xi & Li (2021) 當時**未納入**
  scDblFinder，所以不能從那篇推論「兩者準確度相當」。取交集也不是「最穩」，
  而是提高 specificity、犧牲 sensitivity 的取捨。
- `01_qc.R`：10x 的 0.8% / 1,000 顆標明是上樣經驗法則，會隨平台世代、化學版本、上樣濃度變動。
- `02_cluster.R`：`dbl > 0.5` 那行改成「候選，不是判定」，與 03 的候選／確認兩層流程一致。
- `02_cluster.R`：PC1 從「GBM 幾乎一定是免疫 vs 膠質」改成「本例如此，換資料要自己看載荷」；
  `cor(PC1, nCount)` 的三段門檻標明是本課的診斷經驗值，不是統計定律。
- `05_infercnv.R`：譜系否決的說明從「不可能是腫瘤細胞、一定是 doublet」改成「多半是」，
  並說明程式做的其實是「證據衝突就不下結論」。

### 文件同步

- **`R/練習手冊.md` 的投影片頁碼修正八處**（03、05、06a、06b、07、08、09、10 全部是舊版頁碼，
  學生照著翻會翻錯頁），並修掉「與作者標籤與作者標籤比對」的重複字。
- `R/練習手冊.md` 的「重跑結果應完全一致」改成有條件的說法，與首頁一致。
- 首頁與 `R/README.md` 的「與作者標籤對答案」改成「比對一致性」。

### 說明文件

- **練習題新增〈把環境鎖起來（renv）〉**：課程在兩個地方要求用 renv，卻沒教過怎麼用——
  補上三行指令、`snapshot()` 靠掃程式碼找套件所以要先載入過、GitHub 套件要確認有沒有鎖到
  `RemoteSha`（DoubletFinder / CellChat / MuSiC 都在 GitHub，分支會移動）、
  `renv/library/` 不上傳、以及「鎖了不等於驗證過，要找乾淨環境 restore 一次」。
  也講明課程本身目前還沒附 lockfile——要求學生做的事，自己也還在做。
- **新增 `R/README.md`**：點進 `R/` 資料夾就看得到十二支腳本各做什麼、執行順序、依賴關係、
  完整版與練習版的差別，以及 JAGS 這類前置需求。（原本那裡放的是倉庫首頁 README 的複本，
  相對連結從 `R/` 出發全部失效。）

### 我需要重跑嗎？

用舊版跑過以下章節的話，建議重跑：

- `02_cluster.R`（只有印出的 cutoff 數字會變，分群結果不變）
- `03_annotate.R`（細胞型別欄位的內容會變）
- `06a_pseudobulk_gsea.R`（熱圖與摘要表）
- `08_trajectory.R`、`09_activity.R`、`10_deconv_survival.R`

`00`、`01`、`04`、`05`、`06b`、`07` 沒有改動，不需要重跑。

---

## v1.0.0 — 2026-09-07

Q 系列首次公開：三集投影片（中英各一套，共 228 頁）、12 支 R 腳本（完整版與練習版）、
23 題實作練習、88 題自測題庫、4 份參考庫。
